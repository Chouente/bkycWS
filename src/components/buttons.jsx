import React from 'react';
import '../styles/bouton.css'

const buttons = (prop) => {
    return (
        <div className='bouton'>
            <button>{this.props.bouton_name} </button>
        </div>
    );
};

export default buttons;